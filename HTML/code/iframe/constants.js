// constants
const actions = {
  videoInfo: 'videoInfo',
};
// can't use localhost if u serve your html in 127.0.0.1
// the origin should be the same as where your server serve at.
const origin = 'http://localhost:8080';
// const origin = 'http://127.0.0.1:8080';