// 求和路径
/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} root
 * @param {number} sum
 * @return {number}
 */
// 1. 深度遍历 回溯退回当前值是否参与到sum中
var pathSum = function(root, sum) {
  
};